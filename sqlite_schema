CREATE TABLE user (
    id SERIAL PRIMARY KEY, 
    login_name TEXT NOT NULL, 
    password TEXT NOT NULL, 
    name TEXT
);

CREATE TABLE tip (
    id SERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    url TEXT, 
    description TEXT,
    user_id INTEGER
);
